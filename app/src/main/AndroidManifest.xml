<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.renaninfo.chegoupacote">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LoginDecisaoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UsuarioActivity"
            android:label="@string/meus_pacotes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdminActivity"
            android:label="@string/admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EntregadorActivity"
            android:label="@string/entregas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UnidadeEditActivity"
            android:label="@string/title_activity_unidade"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UsuarioSearchActivity"></activity>
    </application>

</manifest>